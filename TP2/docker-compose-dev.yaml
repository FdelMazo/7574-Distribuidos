version: '3'
services:
  feeder:
    image: feeder:latest
    networks:
      - testing_net
    volumes:
      - ./config.ini:/config.ini:ro
      - ./data:/data
    environment:
      - TEST_LINES=5
    profiles: ["feeder"]

  base_node: &base_node
    image: node:latest
    networks:
      - testing_net
    profiles: ["graph"]
    volumes:
      - ./config.ini:/config.ini:ro

  posts_worker:
    <<: *base_node
    environment:
      - NODE_TYPE=posts_worker
    deploy:
      mode: replicated
      replicas: ${POSTS_WORKERS:-1}

  comments_worker:
    <<: *base_node
    environment:
      - NODE_TYPE=comments_worker
    deploy:
      mode: replicated
      replicas: ${COMMENTS_WORKERS:-1}

  source:
    <<: *base_node
    environment:
      - NODE_TYPE=source

  posts_collector:
    <<: *base_node
    environment:
      - NODE_TYPE=posts_collector
    depends_on:
      - posts_worker

  server:
    image: server:latest
    networks:
      - testing_net
    volumes:
      - ./config.ini:/config.ini:ro
    profiles: ["query"]

  client:
    image: client:latest
    networks:
      - testing_net
    volumes:
      - ./config.ini:/config.ini:ro
    profiles: ["query"]
    depends_on:
      - server

networks:
  testing_net:
    ipam:
      driver: default
      config:
        - subnet: 172.25.125.0/24
